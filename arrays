#include<stdio.h>
int main()
{
	int a[10],i,j,l=0,s=0;
	for(i=0;i<10;i++)
	{
		scanf("%d",&a[i]);
	}
	l=a[0];
	s=a[0];
        for(j=1;j<10;j++)
	{
	if(a[j]>=l)
	{
		l=a[j];
	}
	else if(a[j]<=s)
	{
		s=a[j];
	}
	}
	printf("biggest number is %d\n",l);
	printf("smallest number is %d",s);
}

